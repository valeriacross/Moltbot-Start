# Dipendenze MoltBot
# Aggiornato 12/07/2026 — versioni verificate contro PyPI in questa sessione.
# pyTelegramBotAPI pinnato a 4.34.0 (ultima disponibile) su scelta esplicita di
# Walter — salto da 4.31.0 (versione precedente, testata) a 4.34.0, NON testato
# su questo stack. Se un deploy si rompe dopo questo bump, primo sospetto qui:
# tornare a ==4.31.0 e verificare il changelog ufficiale per breaking change.
#
# 12/07/2026 — controllo versioni più recenti su PyPI, pacchetto per pacchetto:
# flask e openpyxl già alla versione più recente, nessuna modifica.
# Pillow: 12.2.0 -> 12.3.0 (patch di sicurezza, CVE-2026-4775 su libtiff).
# google-genai: 2.6.0 -> 2.11.0 (salto di 5 versioni minori). Changelog
# ufficiale controllato riga per riga da 2.6.0 a 2.10.0 (2.11.0 non ancora
# comparso nel changelog al momento del controllo): zero breaking change nel
# range, solo aggiunte (nuovi campi/tool) e fix su funzionalità non usate da
# questi bot (Live API, Interactions API, Computer Use, video). L'unica nota
# rilevante è nella 2.9.0: "l'implementazione delle Interactions è stata
# completamente sostituita, la superficie pubblica dell'API resta invariata"
# — refactor interno, non visibile per chi chiama generate_content() in
# forma base come fanno questi bot. Se un deploy si rompe dopo questo bump,
# primo sospetto qui: tornare a >=2.6.0 e controllare il changelog di 2.11.0
# specificamente, che non era ancora disponibile al momento di questo bump.
pyTelegramBotAPI==4.34.0
flask==3.1.3
Pillow>=12.3.0
google-genai>=2.11.0
openpyxl>=3.1.5
